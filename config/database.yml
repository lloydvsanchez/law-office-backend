# PostgreSQL. Versions 9.3 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On macOS with Homebrew:
#   gem install pg -- --with-pg-config=/usr/local/bin/pg_config
# On Windows:
#   gem install pg
#       Choose the win32 build.
#       Install PostgreSQL and put its /bin directory on your path.
#
# Configure Using Gemfile
# gem "pg"
#
default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see Rails configuration guide
  # https://guides.rubyonrails.org/configuring.html#database-pooling
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  url: <%= ENV["DATABASE_URL"] %>

development:
  <<: *default
  username: <%= ENV["DATABASE_USERNAME"] %>
  password: <%= ENV["DATABASE_PASSWORD"]%>
  host: <%= ENV["DATABASE_HOST"]%>
test:
  <<: *default

production:
  primary:
    <<: *default
    url: <%= ENV['DATABASE_URL'] %>
    prepared_statements: false
    pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
    
  deploy:
    <<: *default
    url: <%= ENV['DATABASE_MIGRATION_URL'] %>
    prepared_statements: false
    database_tasks: true

  cable:
    <<: *default
    url: <%= ENV["DATABASE_URL"] %>
    prepared_statements: false
    migrations_paths: db/cable_migrate

  queue:
    <<: *default
    url: <%= ENV["DATABASE_URL"] %>
    prepared_statements: false
    migrations_paths: db/queue_migrate

  cache:
    <<: *default
    url: <%= ENV["DATABASE_URL"] %>
    prepared_statements: false
    migrations_paths: db/cache_migrate

#  primary: &primary_production
#    <<: *default
#    database: law_office_production
#    username: law_office
#    password: <%= ENV["LAW_OFFICE_DATABASE_PASSWORD"] %>
#  cache:
#    <<: *primary_production
#    database: law_office_production_cache
#    migrations_paths: db/cache_migrate
#  queue:
#    <<: *primary_production
#    database: law_office_production_queue
#   migrations_paths: db/queue_migrate
#  cable:
#   <<: *primary_production
#   database: law_office_production_cable
#   migrations_paths: db/cable_migrate
